<!DOCTYPE html>
<html lang="en">
<head>
    <title>Cockpitdecks Web Decks</title>
<style type="text/css">
body {
    font-family: sans-serif;
}
</style>
</head>
<body>
    <h2>Welcome to Cockpitdecks Web Decks</h2>
    {% if virtual_decks|length > 0 %}
        <p>List of available web decks:</p>
        <ul id="decklist">
        {% for deck in virtual_decks %}
            <li><a href="#"
                   onclick="window.open(
                        '/deck/{{ deck|e }}',
                        '{{ deck }}',
                        'height=200,width=200,resisable=0,toolbar=0,locationbar=0,menubar=0,statusbar=0,personalbar=0,scrollbars=0'
                   );"
                   target="_blank"
            >{{ deck }}</a></li>
        {% endfor %}
        </ul>
        <p>Deck will start in a new window.</p>
    {% else %}
        <p>Please start Cockpitdecks to see available web decks.</p>
        <p><a href="/">Refresh page</a></p>
    {% endif %}
</body>
</html>